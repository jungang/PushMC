——首页——
请俊岩设计一下UI
首页数据自定义目前还没有



——数据源——
新建数据
1）webhooks 的更新时间，也应该是分钟；
(去掉定时更新)


——数据标注——
属性字段都是一级？
数据源标注的列表筛选，目前没有与表关联；
(小宁接口更新)




——标签分类——
平台管理员创建分类，分类目前是“业务频道”是固定默认分类，所有业务频道的推送标签，都只能选择‘业务频道’分类下的标签；
默认分类-业务频道
默认分类-内容




——业务频道——
新建业务频道
1）选择数据来源，切换数据来源的时候，点击加入已选，才保留已选数据表，否则切换就刷新；
2）点击“加入已选” 判断一下，保留一条记录，现在是点几次显示几条；（bug）
3）业务频道名称，建议不要默认***，不知道为什么点下一步没反应；（主表选择提醒）常量
（bug-标签显示）


——推送内容——
推送内容的筛选下拉框里面，没有业务频道（筛选项 去掉’业务频道‘）
新建内容 保存后，提示数新建据源成功，应该改“保存成功”（bug）
新建内容，把使用新闻链接，位置调整了一下，麻烦更新
新建内容，审批状态，在创建页面可以不显示，创建后查看在显示；（审批中的不能修改，审批人打开该页面视为审批中）（补充业务规则）


——推送对象、推送模版——
与消息推送的设置页面整合了～


———消息推送———
新建推送
1）频道标签不能选？没看到下拉框
2）这个页面的发送对象也有变化，查看原型
3）消息模版的文本消息，貌似也不太对哦～
4）高级选项，上次会议取消了
不要富文本，简单文本框

新建频道推送、渠道推送的交互，请俊岩看一下～


频道订阅 交互行为



http://localhost:8080/dashboard?token=2#/business-channel
